# ========================================
# Variables de Entorno - Backend ML Litper Logística
# ========================================
# IMPORTANTE: Copia este archivo a .env en la carpeta backend
# y reemplaza los valores con tus configuraciones reales.
# NUNCA subas el archivo .env con valores reales a git.

# ==================== Base de Datos ====================
# PostgreSQL - Usa la URL completa proporcionada por Railway/Render/local
DATABASE_URL=postgresql://litper_user:litper_pass@localhost:5432/litper_ml_db

# Pool de conexiones (opcional, valores por defecto)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800
DB_ECHO_SQL=false

# ==================== API de Claude (Anthropic) ====================
# Obtén tu API key en: https://console.anthropic.com/
CLAUDE_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxx
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxx

# Modelo de Claude a usar
CLAUDE_MODEL=claude-sonnet-4-20250514

# ==================== Configuración de ML ====================
# Directorio donde se guardan los modelos entrenados
MODELOS_DIR=ml_models/saved

# Directorio para archivos subidos
UPLOADS_DIR=uploads

# Habilitar reentrenamiento automático (true/false)
REENTRENAMIENTO_AUTOMATICO=true

# Umbral de días para considerar retraso
DIAS_RETRASO_ALERTA=3

# Umbral de probabilidad para alertar
UMBRAL_PROBABILIDAD_RETRASO=0.7

# Mínimo de registros para entrenar
MIN_REGISTROS_ENTRENAMIENTO=100

# ==================== Servidor API ====================
# Host y puerto para el servidor FastAPI
API_HOST=0.0.0.0
API_PORT=8000

# Modo debug (solo para desarrollo)
DEBUG=false

# ==================== Logging ====================
# Nivel de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ==================== Chat Inteligente ====================
# Máximo de tokens para respuestas del chat
CHAT_MAX_TOKENS=2000

# ==================== Configuraciones Adicionales ====================
# Zona horaria
TZ=America/Bogota

# Límite de tamaño de archivo (en bytes)
MAX_FILE_SIZE=52428800

# Días de retención de logs
DIAS_RETENCION_LOGS=30

# ==================== Google Sheets Integration ====================
# Ruta al archivo JSON de credenciales de Google Cloud
GOOGLE_SHEETS_CREDENTIALS_FILE=credentials/google-sheets-credentials.json

# ID del proyecto de Google Cloud (opcional, se toma del JSON)
GOOGLE_CLOUD_PROJECT_ID=teamsync-pro-lw9uc

# Email de la cuenta de servicio (se muestra en el JSON)
# GOOGLE_SERVICE_ACCOUNT_EMAIL=litper-sheets@teamsync-pro-lw9uc.iam.gserviceaccount.com

# IDs de los spreadsheets (se configuran desde la UI)
# GOOGLE_SHEETS_ENVIOS_ID=
# GOOGLE_SHEETS_FINANZAS_ID=
# GOOGLE_SHEETS_DASHBOARD_ID=
